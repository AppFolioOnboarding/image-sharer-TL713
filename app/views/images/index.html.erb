<div align = "center">
  <h1>Image Index</h1>
  <h3> <%= link_to 'Upload image using url', new_image_path %> </h3>
  <h2>List of images</h2>
  <div>
    <% @images.each do |f| %>
      <p>
        <%= image_tag(f.link, width: '400', title: f.title)%>
      </p>
    <% end %>
  </div>
</div>
